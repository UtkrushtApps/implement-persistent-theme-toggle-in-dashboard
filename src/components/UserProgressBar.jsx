import React from 'react';

const UserProgressBar = ({ user, completed, total }) => {
  // To be implemented: Accessible progressbar and labels
  return (
    <div className="user-progress">
      {/* Progress bar UI */}
    </div>
  );
};

export default UserProgressBar;
